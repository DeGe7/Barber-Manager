{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "e2e:typecheck": "tsc -p e2e/tsconfig.json --noEmit",
    "e2e:structure:test": "node --test e2e/check-structure.test.mjs",
    "e2e:structure": "node e2e/check-structure.mjs",
    "e2e:provision:test": "node --experimental-strip-types --test e2e/provision-interruption.test.mjs",
    "e2e:validate": "pnpm run e2e:typecheck && pnpm run e2e:structure:test && pnpm run e2e:provision:test && pnpm run e2e:structure",
    "e2e": "pnpm run e2e:validate && playwright test --config=e2e/playwright.config.ts",
    "e2e:isolated": "E2E_PROVISION=true E2E_START_SERVER=true pnpm e2e"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
